import { makeStyles } from "@mui/styles"

export default makeStyles(() => {
  return {
    footer__container: {
      height: 25,
      width: "100%",
      display: "flex",
      justifyContent: "center",
      alignItems: "center",
    },
  }
})
